# REQUIRED FIX: Pinning core libraries to resolve nested ImportErrors.
# This list is clean and uses known compatible versions for Streamlit Cloud (CPU).

streamlit==1.50.0
numpy==1.26.4
Pillow==11.0.0
pandas==2.3.3
requests==2.32.5
deep-translator==1.11.4
openai==2.8.1
gTTS==2.5.4

# Hugging Face Ecosystem (Pinned versions to ensure compatibility)
transformers==4.40.0
diffusers==0.28.0
huggingface-hub==0.24.0
accelerate==0.30.0
safetensors==0.7.0
tokenizers==0.15.2

# PyTorch (Pinned, generic CPU-compatible versions)
torch==2.1.2
torchvision==0.16.2

# Remaining utilities
pydantic==2.12.4
typing-extensions==4.15.0
tenacity==9.1.2
pydeck==0.9.1
toml==0.10.2
